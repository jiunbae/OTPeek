[bindings.csharp]
# Make generated types public so App / Widget / Core projects can consume them.
access_modifier = "public"
# Stable namespace for the generated bindings (matches otpeek-ffi's uniffi namespace "otpeek").
namespace = "Uniffi.Otpeek"
