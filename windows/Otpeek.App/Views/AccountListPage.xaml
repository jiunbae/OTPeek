<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Otpeek.App.Views.AccountListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Otpeek.App.Views"
    xmlns:vm="using:Otpeek.App.ViewModels">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Accounts"
                       Style="{StaticResource TitleTextBlockStyle}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button x:Name="ScanQrButton" ToolTipService.ToolTip="Scan QR Code">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8B0;" FontSize="16" />
                        <TextBlock Text="Scan QR" />
                    </StackPanel>
                </Button>

                <Button x:Name="AddButton" Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="16" />
                        <TextBlock Text="Add Account" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Search -->
        <AutoSuggestBox x:Name="SearchBox"
                        Grid.Row="1"
                        Margin="0,0,0,16"
                        PlaceholderText="Search accounts"
                        QueryIcon="Find" />

        <!-- Empty State -->
        <StackPanel x:Name="EmptyState"
                    Grid.Row="2"
                    Visibility="Collapsed"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="16">
            <FontIcon Glyph="&#xE8D7;" FontSize="64" Opacity="0.5" />
            <TextBlock Text="No accounts yet"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       Opacity="0.7" />
            <TextBlock Text="Add your first account by scanning a QR code or entering manually"
                       TextWrapping="Wrap"
                       TextAlignment="Center"
                       MaxWidth="300"
                       Opacity="0.5" />
            <Button Content="Add Account"
                    Style="{StaticResource AccentButtonStyle}"
                    Margin="0,16,0,0"
                    Click="OnAddButtonClick" />
        </StackPanel>

        <!-- Account List -->
        <ListView x:Name="AccountListView"
                  Grid.Row="2"
                  SelectionMode="None"
                  ItemsSource="{x:Bind ViewModel.Accounts, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:AccountItemViewModel">
                    <Border Style="{StaticResource AccountCardStyle}"
                            Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Icon: 서비스 로고(파비콘)가 있으면 흰 배경 타일, 없으면 색상 이니셜 -->
                            <Grid Grid.Column="0"
                                  Width="44" Height="44"
                                  Margin="0,0,16,0">
                                <!-- 이니셜 폴백 -->
                                <Border CornerRadius="12"
                                        Background="{StaticResource PrimaryBrush}"
                                        Visibility="{x:Bind InitialVisibility, Mode=OneWay}">
                                    <TextBlock Text="{x:Bind Initial}"
                                               FontSize="18"
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center" />
                                </Border>
                                <!-- 파비콘: 깨끗한 흰 타일 위에 로고(투명 로고 대비) -->
                                <Border CornerRadius="12"
                                        Background="White"
                                        Padding="5"
                                        Visibility="{x:Bind IconVisibility, Mode=OneWay}">
                                    <Image Source="{x:Bind IconSource, Mode=OneWay}"
                                           Stretch="Uniform" />
                                </Border>
                            </Grid>

                            <!-- Account Info -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Issuer}"
                                           Style="{StaticResource IssuerTextStyle}" />
                                <TextBlock Text="{x:Bind AccountName}"
                                           Style="{StaticResource AccountNameTextStyle}" />
                            </StackPanel>

                            <!-- OTP Code (Click to copy) -->
                            <Button Grid.Column="2"
                                    Command="{x:Bind CopyCodeCommand}"
                                    ToolTipService.ToolTip="Click to copy"
                                    VerticalAlignment="Center"
                                    Margin="16,0"
                                    Padding="12,8"
                                    CornerRadius="8"
                                    Background="{x:Bind IsCopied, Mode=OneWay, Converter={StaticResource CopiedBackgroundConverter}}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                                        <Setter Property="BorderThickness" Value="0" />
                                    </Style>
                                </Button.Style>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind FormattedCode, Mode=OneWay}"
                                                   Style="{StaticResource OtpCodeTextStyle}" />
                                        <FontIcon Glyph="&#xE8FB;"
                                                  FontSize="20"
                                                  Foreground="Green"
                                                  Visibility="{x:Bind IsCopied, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </StackPanel>
                                    <ProgressBar Value="{x:Bind Progress, Mode=OneWay}"
                                                 Maximum="1"
                                                 Height="4"
                                                 Margin="0,4,0,0" />
                                </StackPanel>
                            </Button>

                            <!-- Actions -->
                            <StackPanel Grid.Column="3"
                                        Orientation="Horizontal"
                                        Spacing="4">
                                <Button ToolTipService.ToolTip="More options">
                                    <FontIcon Glyph="&#xE712;" FontSize="16" />
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Edit" Icon="Edit"
                                                            Command="{x:Bind EditCommand}" />
                                            <MenuFlyoutItem Text="Favorite" Icon="Favorite"
                                                            Command="{x:Bind ToggleFavoriteCommand}" />
                                            <MenuFlyoutSeparator />
                                            <MenuFlyoutItem Text="Delete" Icon="Delete"
                                                            Command="{x:Bind DeleteCommand}" />
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Loading -->
        <ProgressRing x:Name="LoadingRing"
                      Grid.Row="2"
                      IsActive="False"
                      Width="48"
                      Height="48"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center" />
    </Grid>
</Page>
